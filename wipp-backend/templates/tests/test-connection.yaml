apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wipp-backend.fullname" . }}-test-connection"
  labels:
    {{- include "wipp-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wipp-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
