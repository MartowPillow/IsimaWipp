apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "testfrontend.fullname" . }}-test-connection"
  labels:
    {{- include "testfrontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "testfrontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
